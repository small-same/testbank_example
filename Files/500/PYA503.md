# PYA503 連加計算

**類別：第5類：函式**

## 題目說明
讓使用者輸入兩個整數，呼叫函式 compute(a, b)，回傳從 a 連加到 b 的和並輸出。

## 範例輸入
```
1
10
```

## 輸出結果
```
55
```

## 程式碼
```python
def compute(a,b):
    ans=0
    for i in range(a,b+1):
        ans+=i
    return ans
x = eval(input())
y = eval(input())
print(compute(x,y))
```